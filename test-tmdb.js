const fetch = require('node-fetch'); require('dotenv').config({ path: '.env.local' }); async function run() { const token = process.env.NEXT_PUBLIC_TMDB_ACCESS_TOKEN; const res = await fetch('https://api.themoviedb.org/3/discover/movie?sort_by=popularity.desc&page=1', { headers: { Authorization: 'Bearer ' + token, accept: 'application/json' } }); const data = await res.json(); console.log(data.results.slice(0, 10).map(m => m.title + ' - ' + m.vote_average)); } run();
